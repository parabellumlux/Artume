# Artume OS — Codebase Audit (2026-09-08)

> Working-tree audit against the previous snapshot (audit2026-08-20). All measurements taken fresh this session.

---

## Executive Summary

| Metric | 2026-08-20 | 2026-09-08 |
|--------|-----------|-----------|
| Total LOC (Rust) | ~19,200 | **17,934** |
| Total LOC (Python, incl. tests) | ~8,200 | **12,170** (9,927 non-test / 2,243 tests) |
| Rust tests | 165 passing | **165 passing (0 failures)** |
| Python tests | 276 passing / 8 failed | **276 passing / 8 failed** (same set) |
| Rust compiler warnings | ~4 | **11** (5 deprecated API + 6 dead code) |
| Python pyflakes findings | not measured | **47 across 22 modules** |
| Python files failing py_compile | 0 | **0** |
| TODO/FIXME/HACK (rs+py) | 0 | **0** |
| Production `.unwrap()`/`.expect()` (Rust) | 0 | **0** (all in `#[cfg(test)]`; 5 module-level regex inits are inert) |
| CI workflow | none | **added but currently broken** |
| IPC auth on Unix socket | open issue | **FIXED** — `SO_PEERCRED` UID check |

**Headline findings:**
1. **All uncommitted.** 50 modified files (1,927 ins / 1,213 del) + 17 untracked entries, 9 commits ahead of `origin/main`, nothing pushed. The entire Python test suite, handlers, clips, and several new modules exist **only in the working tree**. This is the single biggest risk.
2. **CI exists but would fail on merge.** `.github/workflows/ci.yml` runs `cargo fmt --check` (currently **fails**, exit 1) and `cargo clippy --workspace -- -D warnings` (would fail on the 11 warnings).
3. **Warnings regressed** (~4 → 11): 5× deprecated `cpal::DeviceTrait::name` + 6 dead-code items.
4. **Python quality debt**: 27 unused imports, 11 placeholder-less f-strings, 9 unused locals.
5. Previous audit's Rust test-distribution table was inaccurate; this audit records **measured** counts (aether-browser is 10, aether-ide is 75 — the 08-20 table had them reversed/shaver away).

---

## 1. Build & Test Health

### Rust (`cargo test --workspace`) — 165 passed, 0 failed

| Crate | Tests (this audit) | Tests (08-20 table) |
|-------|--------------------|---------------------|
| aether-ide | **75** | 16 ✗ (was mislabeled) |
| aether-orchestrator | 21 | 21 |
| aether-buffer | **20** | 16 |
| aether-attention | **16** | 10 |
| aether-audio | **16** | 20 |
| aether-browser | **10** | 75 ✗ (was mislabeled) |
| aetherfs-core | 7 | 7 |
| aetherfs-cli / aetherfs-proto / daemon / shell / tts_test bins | 0 | 0 |

### Python (`pytest`) — 276 passed, 8 failed

Failures are unchanged from 08-20: `bluetooth_manager` ×2, `ide_client`, `power_manager`, `state_manager`, `window_manager` ×3. All unrelated to recent work.

### Compilation & linting

| Check | Result |
|-------|--------|
| `cargo build --workspace` | ✅ clean |
| `py_compile` (all modules) | ✅ clean |
| `cargo fmt --check` | ❌ **exits 1** (diffs in `aether_attention`, formatting drift) |
| `cargo clippy -- -D warnings` | ❌ would fail (see §2) |
| `python -m pyflakes` | ⚠️ 47 findings (§3) |

---

## 2. Compiler Warnings (11 distinct, up from ~4)

| File | Warning | Type |
|------|---------|------|
| `aether_audio/src/capture.rs:34` | `cpal::DeviceTrait::name` deprecated | deprecated API |
| `aether_audio/src/output.rs:53,75` | `cpal::DeviceTrait::name` deprecated | deprecated API |
| `aether_audio/src/wake_word.rs:137,159` | `cpal::DeviceTrait::name` deprecated | deprecated API |
| `aether_orchestrator/src/conversation.rs:121` | field `spatial_mixer` never read | dead code |
| `aether_ide/src/parser.rs:182` | field `language` never read | dead code |
| `aetherfs-core/src/classifier/onnx.rs:71` | `has_minilm()` never used | dead code |
| `aetherfs-core/src/extract.rs:20-22` | `ContentChunk::char_start/char_len` never read | dead code |
| `aetherfs-core/src/index/mod.rs:61` | `classifier()` never used | dead code |
| `aetherfs-core/src/index/sqlite.rs:271` | `search_chunks()` never used | dead code |

The 5 cpal warnings are a quarter-year-old known migration (`.name()` → `.description()`). The 6 dead-code warnings are public API kept for future use — acceptable but will block `clippy -D warnings`.

---

## 3. Python Static Analysis (pyflakes — 47 findings / 22 modules)

| Category | Count |
|----------|-------|
| Unused imports | 27 |
| f-strings without placeholders | 11 |
| Unused locals | 9 |

Top offenders: `intent_router.py` (9), `trigger_engine.py` (6), `audio_engine.py` (3), `artume_skills.py` (3), `artome_ide/terminal_engine.py` (3), `artome_ide/ai_assistant.py` (3).

Notable: `audio_engine.py:111-112` — `is_speaking`/`silent_chunks` assigned in `listen()` then shadowed/re-initialized in `_listen_loop()`; harmless but confusing. `trigger_engine.py, artume_skills.py` locals assigned but never used suggest dead branches.

---

## 4. Security Posture

| Issue | Status |
|-------|--------|
| No IPC auth on Unix socket (08-19 finding) | **FIXED** — `SO_PEERCRED` UID check in `aether_ide/src/ipc.rs` (~line 93). Any local process connecting must match the daemon's UID. ⚠️ degrades open if `getsockopt` fails (uid `None` allowed through) — minor hardening gap |
| `shell=True` | Only `artome_ide/terminal_engine.py:23` — **by design** (voice-driven terminal executes user commands). Intentional, but note it |
| Hardcoded secrets/PINs | **None found** in scan. `credential_vault` uses voice-collected PIN |
| WiFi password in `ps` | Still fixed (temp-file pass-through) |
| Rust shell out | `system_commands.rs` uses list-form `Command::new` — no shell interpretation |
| API keys | `weather_service.py` reads `OPENWEATHER_API_KEY` env var; `repositories.md` remains gitignored |
| Binary/asset hygiene | `piper/`, `models/`, `*.onnx` fully gitignored (fix from commit `3ccf08f`) |

---

## 5. Changes Since 2026-08-20

### New modules

| Module | LOC | Notes |
|--------|-----|-------|
| `aether_orchestrator/src/system_commands.rs` | 479 | Volume/bt/wifi/audio/timer/status commands (untracked) |
| `lsp_client.py` | 305 | LSP (JSON-RPC stdio) + DAP (TCP) Python clients (untracked) |
| `clip_registry.py` | 276 | Clip lookup + duplicate stripping (untracked) |
| `handlers/modes.py` | 215 | Mode routing (untracked) |
| `state_manager.py` | 126 | Persistent app state (untracked, 1 test failing) |
| `confirmation_dialog.py` | 99 | Voice yes/no confirmations (untracked) |
| `power_manager.py` | 97 | Battery/power (untracked, 1 test failing) |
| `scripts/generate_clips.py` | 106 | Generates the 23 ack clips |
| `.github/workflows/ci.yml` | 70 | CI — see §1 (currently red) |
| `aether_ide/src/queries/` | 5 | tree-sitter queries now populated (go, js, json, python, rust) — empty since 08-19 |

### Notable modifications

- `start.sh` — full `--python` mode: venv, deps, piper/onnx/aplay/amixer checks, clip-count check (23), warm-up refactor.
- `aether_orchestrator/src/conversation.rs` — skill-registry dispatch integrated into the turn pipeline (file skills first, then builtin, then LLM prompt-template skills); `current_mode` tracked.
- `aether_orchestrator/src/skills/mod.rs` — `SkillContext` made `Arc`-based, real LLM execution for prompt-template skills, dispatched; removed the last `.expect("HOME")`.
- `aether_ide/src/ipc.rs` — `lsp_init`/`dap_init` RPC handlers + `SO_PEERCRED` auth.
- `aetherfs-core` — UTF-8-safe chunking in `extract.rs` (char-boundary snapping, no mid-codepoint slicing); optional `pdf-extract` feature + `libc` dep; minor gRPC/sqlite/profile fixes.
- `artome_core.py` — heavy refactor (~1,000 lines diff), clips + triggers + state integration.
- `audio_engine.py` — VAD listener refactor per above; `quieter`/`softer` synonyms added to Rust volume parser.
- Rust dependency hygiene: `cpal` unified to 0.17; poison-safe mutex locks (`unwrap_or_else(into_inner)`) added across capture/buffer/attention.

---

## 6. Blocking / High-Priority Findings

### P0 — Everything is uncommitted
50 modified files + 17 untracked entries, 9 commits ahead of origin with nothing pushed. The Python tests (22 files / ~280 tests), handlers, clips, `system_commands.rs`, `state_manager.py`, `power_manager.py`, `clip_registry.py`, `lsp_client.py`, `confirmation_dialog.py`, tree-sitter queries, and CI config do **not exist in git**. One `git clean -fd` or disk failure loses months of work.

**Next step:** `git add` the intended files/dirs, commit in logical units, push to origin.

### P1 — CI would fail on first run
`cargo fmt --check` exits 1 and `clippy -- -D warnings` trips on 11 warnings. Either land formatting + warning fixes first, or relax the clippy gate to `-D` on a curated allowlist.

### P1 — Warnings regressed to 11
5× deprecated `cpal::name()` (known migration target) + 6 dead-code items. Fixing the dead fields/methods is a churn-free ~30-minute pass.

### P2 — Python quality debt
47 pyflakes findings. Low risk individually, but the 11 placeholder-less f-strings indicate copied/pasted strings in `intent_router.py` error paths worth a look.

### P2 — IPC auth fall-open path
If `SO_PEERCRED` fails, connections are accepted. Consider rejecting when the credential can't be obtained (fail closed).

---

## 7. Success Criteria Status (unchanged from 08-20)

All 10 core voice success criteria remain ✅ as of the last audit; no regressions detected in tests.

---

*Generated: 2026-09-08 by opencode (working-tree audit)*