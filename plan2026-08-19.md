# Artume OS — Development Plan (2026-08-19)

> Next steps after the Phase 0 audit fixes. Prioritized by impact on the project's core mission: a voice-native OS for blind users.

---

## Phase 0: Critical Fixes (COMPLETE)

All 8 items done. 152 tests passing, warnings reduced from 42 → 16.

| # | Task | Status |
|---|------|--------|
| 0.1 | Add `lsp_init` IPC handler | Done |
| 0.2 | Add `dap_init` IPC handler | Done |
| 0.3 | Replace 6 `.expect()` with Result/fallback | Done |
| 0.4 | Replace 16 mutex `.unwrap()` with poisoned-lock recovery | Done |
| 0.5 | Fix broken bluetooth device detection | Done |
| 0.6 | Add audio buffer overflow logging | Done |
| 0.7 | Clean up compiler warnings (42 → 16) | Done |
| 0.8 | Delete ~250 lines of commented-out tool-calling code | Done |

---

## Phase 1: Wire the IDE (HIGH IMPACT)

**Goal:** Make the Audio-First IDE actually usable from the Python voice UI.

| # | Task | Description | Files | Effort |
|---|------|-------------|-------|--------|
| 1.1 | Wire earcons to code navigation | Call `play_earcon()` from `artome_ide/earcons.py` during skim/read/navigate | `artome_core.py`, `artome_ide/__init__.py` | Medium |
| 1.2 | Wire spatial audio workspace | Connect `SpatialMixer` to actual audio playback during code navigation | `aether_audio/src/spatial_mixer.rs`, `artome_core.py` | Medium |
| 1.3 | Start aether_ide daemon from Python | Auto-launch daemon on startup, connect via `IdeClient` | `artome_core.py`, `artome_ide/__init__.py` | Medium |
| 1.4 | Wire AI assistant to intent router | Connect `ai_assistant.py` for "fix this"/"explain" commands | `artome_core.py`, `intent_router.py` | Small |
| 1.5 | Wire Git to intent router | Connect `git_engine.py` for "git status"/"commit" commands | `artome_core.py`, `intent_router.py` | Small |
| 1.6 | Wire terminal to intent router | Connect `terminal_engine.py` for "run tests"/"run file" commands | `artome_core.py`, `intent_router.py` | Small |

---

## Phase 2: Infrastructure (P0 Features)

**Goal:** Core infrastructure that every other feature depends on.

| # | Task | Description | Effort |
|---|------|-------------|--------|
| 2.1 | Credential Vault | Encrypted storage for IMAP/SMTP/WiFi credentials. Voice PIN unlock. `keyring` or `age` encryption. | Large |
| 2.2 | Notification Center | Async delivery with focus-aware queuing. Batch summarize on idle. | Medium |
| 2.3 | Confirmation Dialog | "Are you sure?" pattern for destructive actions (delete, send, shutdown). | Small |
| 2.4 | Persistent State | Save/restore modes, history, bookmarks, open files across restarts. SQLite or JSON. | Medium |
| 2.5 | Window Manager | List, focus, minimize, maximize, close windows via EWMH/NetWM (`xdotool`, `wmctrl`). | Medium |
| 2.6 | App Launcher | Desktop file parser. Launch, quit, list running apps. | Medium |
| 2.7 | Clipboard Manager | Copy, paste, select all, clipboard history via `xclip`/`xsel`. | Small |

---

## Phase 3: System Control

**Goal:** Full system management through voice.

| # | Task | Description | Effort |
|---|------|-------------|--------|
| 3.1 | Bluetooth Manager | Fix broken detection (done in Phase 0), add connect/disconnect/scan/pair | Medium |
| 3.2 | WiFi Manager | List networks, connect, disconnect, save credentials via `nmcli` | Medium |
| 3.3 | Audio Output Switcher | List PulseAudio sinks, switch default via `pactl` | Small |
| 3.4 | Power Management | Shutdown, restart, sleep, lock screen via `systemctl` | Small |

---

## Phase 4: Testing & CI

**Goal:** Confidence in changes, automated quality gates.

| # | Task | Description | Effort |
|---|------|-------------|--------|
| 4.1 | Python test suite | Unit tests for all 15+ Python modules. Use `pytest`. | Large |
| 4.2 | Rust integration tests | End-to-end tests for the IPC server (connect, send request, verify response). | Medium |
| 4.3 | LSP/DAP e2e tests | Tests with real LSP servers (`pylsp`, `rust-analyzer`) and debug adapters. | Medium |
| 4.4 | CI/CD pipeline | GitHub Actions: `cargo test`, `cargo clippy`, `pytest` on push/PR. | Medium |
| 4.5 | Populate tree-sitter queries | Create `.scm` files for Python, Rust, JS, TS, Go, JSON in `aether_ide/src/queries/`. | Medium |

---

## Phase 5: Polish & Usability

**Goal:** Speed, reliability, and comfort for daily use.

| # | Task | Description | Effort |
|---|------|-------------|--------|
| 5.1 | TTS speed optimization | Support 300-400 wpm for experienced users. Abbreviation mode. | Medium |
| 5.2 | User profile learning | Learn preferred reading speed, common navigation patterns, frequent files. | Medium |
| 5.3 | Onboarding wizard | "Say 'help' to hear available commands." Progressive disclosure. | Medium |
| 5.4 | Error recovery | "Undo" reverts last edit. "Go back" returns to previous location. | Small |
| 5.5 | Deprecated cpal API migration | Migrate 5 `name()` calls to `description()`/`id()`. | Small |

---

## Design Principles (from PLAN_ARTUME_OS.md)

1. **Voice-native, not voice-bolted-on.** Every action has a voice path.
2. **Confirm before destroy.** Delete, send, shutdown — always confirm.
3. **Narrate, don't silence.** "Fetching that page..." — user should never wonder.
4. **Summarize, don't dump.** Long content gets condensed.
5. **Remember, don't ask again.** Learned credentials, preferences, patterns.
6. **Queue, don't interrupt.** Focus-aware notification delivery.
7. **Barge-in always works.** User speaks → Artume stops and listens.
8. **Mode-aware context.** "Read this" means different things in different modes.

---

## Success Criteria

The project is complete when a blind user can:

1. Wake Artume with "Hey Artume"
2. Check email, browse web, manage files — all by voice
3. Read and edit code in the Audio-First IDE with spatial audio
4. Debug with breakpoints, step-through, variable inspection
5. Use Git, run tests, manage terminal — all by voice
6. Never need to see a screen, touch a keyboard, or use a mouse

---

*Generated: 2026-08-19 by opencode*
